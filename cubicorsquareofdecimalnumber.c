#include <stdio.h>
int main() {
    float n;
    printf("Enter the value of n:");
    scanf("%f",&n);

    printf("The Square of the given decimal value is %2f\n", n*n );
    printf("The cube of the given decimal number is %2f\n", n*n*n);

    return 0;
